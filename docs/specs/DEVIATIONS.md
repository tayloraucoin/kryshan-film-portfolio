# Deviations (append-only)

One line per intentional divergence from a spec or a house convention. Never rewrite history. Format:

```
YYYY-MM-DD · <ticket-id> · <what changed> · <why>
```

2026-09-22 · CB-0 · shadcn is mandated here although taylor-aucoin's CLAUDE.md bans it · that ban protects one bespoke site; client sites are many and their chrome must be cheap and consistent. The look is the kit's, not shadcn's.
2026-09-22 · CB-0 · `lib/cn.ts` exists although catch-all helper files are banned · shadcn's generated components import `cn` from one alias; the file does one thing and is named for it.
2026-09-22 · CB-0 · Site identity (`SITE`) lives in `lib/config.ts`, not `content/` · matches both sibling repos and keeps `lib/metadata.ts` from importing upward.
2026-09-22 · CB-1 · Three placeholder kits, layouts and mocks ship in the boilerplate · the machinery must be runnable before a client's deliverables exist; each placeholder renders a ribbon and Batch 7 replaces it (REVIEW-LAYER.md §5).
2026-09-22 · CB-0 · `components/` split into `primitives/` (shadcn output) and `composed/` (ours), matching CC's `@cc/ui` tiers · Taylor's feedback after CB-1; the flat `ui/` folder hid which components carried decisions.
2026-09-22 · KR-0 · Repo created as `kryshan-film-portfolio` from `client-boilerplate` per NEW-CLIENT.md; agent ports 4500/4510; Vitrine's deliverables filed under `docs/client/`; 27 posters converted into `public/media/posters/` ahead of KR-1 · so no build thread depends on chat attachments. Six poster mappings are marked *likely* in `docs/client/project-images/MANIFEST.md` and need confirmation.
2026-09-22 · KR-1 · Site identity extended in `SITE` (`lib/config.ts`: `place`, six `social` entries with `secondary`), not `content/site.ts`; the prompt's `rolesLine` is the existing `SITE.tagline` (same string) · CB-0 put identity in `SITE` so metadata never imports upward. `content/site.ts` exists for shared copy only (`NAV_LABELS`, `STRANDS` from 03 §8) so KR-2/KR-3 find it.
2026-09-22 · KR-1 · `Project.story` is optional and absent on all 27 entries · the prompt says "the Story text from 02, verbatim", but 02 §3.5 has no story column (03 says the 27 stories exist in the intake, which is not in the repo). The expanded cell renders story when present. Awards come from 02 §8.1, cut to one award and two festivals (03 §9); The Bully Solution carries festivals only (award list conflicts, 02 §13 #6).
2026-09-22 · KR-1 · `Project.poster` is `{ src, width, height }`, not a path string; `embed` is a discriminated union (`youtube|vimeo` + id, `linkout` + url, `none`) · `VideoEmbed` needs real dimensions, and eight posters are not 16:9; the union makes a link-out without a URL unrepresentable.
2026-09-22 · KR-1 · Kit A lives in `ReviewKit` (`review/kits/types.ts`), not `brand/kit.ts`: essence in `tagline`, PDF A-5 never-list in `never`, PDF thesis/roles/type scale/voice in their slots. No `extraVars`: hairlines are `border-border/40` · the repo's type, not the prompt's guess, is the contract. All three font slots point at Archivo (PDF: "No serif, no mono, no display face").
2026-09-22 · KR-1 · Small red text ("Passion project" label, "Watch ↓", links, hovers) uses `accent` (red 300), not `primary` as Deliverable 4 says in places · the prompt's own acceptance criterion and the PDF (red 500 on black is 3.4:1, AA large only). `primary` is used only for the wordmark (20 px 800) and the display name.
2026-09-22 · KR-1 · Ids are `kryshan-a` (kit, layout) and `home-a` (mock), replacing `kit-a`/`layout-a`/`mock-a` · the routes the ticket names. The mock component is `review/mocks/home-a.tsx`; the page, not the mock, applies `KitScope`.
2026-09-22 · KR-1 · The frame-level "Frame to be replaced" / "Link pending" marker is a new `review/mocks/_components/frame-ribbon.tsx`, not the existing `PlaceholderRibbon` · that one lives in `app/review/` (review/ may not import it) and marks a whole page; same amber so the two read as one convention.
2026-09-22 · KR-1 · `VideoEmbed` gained `startPlaying`; the kit page's contrast table gained "Small accent on ground" and renamed two rows ("Primary on ground", "Text on primary") · layout A §2 is one tap to play, so the player an opened cell creates starts playing; the prompt's contrast rule is about `accent`, which the table did not print.
2026-09-22 · KR-1 · The Wolf of West Georgia Street renders on Home with `rights: "pending"` · layout A §4 places it and 02 §14 Q13 assumes it is cleared; layout A §7's "pending renders as names" is overridden by 02 (source of truth) until he answers.
2026-09-22 · KR-1 · The expanded cell also carries his email beside "Full page →" · layout A §2 ("the expanded cell carries … his email link"); the prompt's panel list omits it.
2026-09-22 · KR-1 · Posters were not converted in this ticket; KR-0 already placed 27 JPEGs · see the KR-0 line above. Link-out URLs are the old site's `/project/<slug>/` pages (02 §1.4), scheme https assumed.
2026-09-22 · KR-1 · `REVIEW_GATE` (`on` default / `off`) added to `lib/env.ts`, honoured in `proxy.ts`; off admits every `/review` request and forwards `/review/access` to `next`; the index warns while off · Taylor's instruction, so local development and agent browser walks need no code. `.env.local` runs with it off. Not in the prompt or CB-1.
2026-09-22 · KR-2 · Kit B's text-size red is `accent` = red 600 (6.2:1); the PDF's note that red 500 "passes AA at body size" on cream is wrong (4.1:1) and was not carried into the kit · the prompt's correction and `lib/color/contrast.ts` agree. `primary` (red 500) is used only at 24 px and up: the wordmark, the hero phrase.
2026-09-22 · KR-2 · Kit B extras are `extraVars` `--surface-dark` (neutral 900) and `--font-quote` (Fraunces Italic, `opsz`, `preload: false`); no inverse slot exists in `BrandKit` · BRANDING.md §3a is the extension point; the fourth face is below the first screen, so it is not preloaded.
2026-09-22 · KR-2 · The hero line sits above the reel strip as the page's h1 (H1 size, Work Sans 500, the phrase in `primary`), not in an overlay on the first slide at display size · an overlay would sit on the play button and over a playing iframe; the prompt marks it optional. Its "twenty-seven years" is unconfirmed (02 §13 #3).
2026-09-22 · KR-2 · The strip's "Info" panel shows year · role · client and awards; no project has a `story` yet · see the KR-1 story line. It renders `story` when one is added.
2026-09-22 · KR-2 · The lightbox player is poster-first (a second tap starts it), while layout B §7 says the iframe mounts on open · the prompt's acceptance criterion ("no iframe until play") governs. `ProjectPlayer` gained `posterFirst` for it.
2026-09-22 · KR-2 · No press quote appears in the lightbox · the prompt limits quotes to Jack and The Bully Solution, neither is in the featured four, and `content/projects.ts` gained no quote field (layout B §7's optional `quote` waits for sourced wording).
2026-09-22 · KR-2 · The lightbox re-applies kit B with `KitScope` inside the dialog popup · the vendored dialog portals to `<body>`, outside the page's kit scope; the primitive is untouched.
2026-09-22 · KR-2 · `VideoEmbed` gained `onPlay` · the strip hides the Directors Reel "Frame to be replaced" ribbon once that player is running.
2026-09-22 · KR-3 · Index role tags follow `content/projects.ts` `roles` (02 §3.5), not layout C §4's shorthand: 5Rhythms and TUTS 2026 show Camera + Editing, Wolf shows Directing + Editing, Dare shows all three · 02 is the source of truth; the layout's per-row tags disagree with his own credits.
2026-09-22 · KR-3 · Tag colours are `extraVars` `--tag-directing|camera|editing`, read only by `expanding-index.tsx` (dots, and `--lit` on the index's column labels and the open row's role label while a row is open); "the page's active labels" is read as the index's labels, not the server-rendered nav · the nav has no active state on a single-page mock, and the wordmark stays green.
2026-09-22 · KR-3 · The index row expands and plays on the one tap (as layout A), not poster-first · layout C §2 ("tap a row (one tap) → it expands and plays"); no iframe exists before that tap. Prompt C's "poster-first video-embed" is read as the row being the poster.
2026-09-22 · KR-3 · No duration column · `content/projects.ts` has no duration field and the prompt forbids inventing one. Lane drops into the title cell's second line below 1280 (layout C §6).
2026-09-22 · KR-3 · The statement uses prompt C's support line (with "IATSE 669.") and omits layout C's separate small line "IATSE 669 · Vancouver · works anywhere." · the same facts twice in one screen.
2026-09-22 · KR-3 · Kit C's three font slots all point at Chivo; Newsreader Italic (`opsz`, not preloaded) is `--font-quote` · PDF C: "No mono, no condensed"; quotes appear on no first screen.
2026-09-22 · KR-3 · The view-transition helper moved to `review/mocks/_components/view-transition.ts` (used by A's grid and C's index); `review/mocks/placeholder-home.tsx` deleted · two consumers; PRIMER says delete the placeholder after KR-3.
2026-09-22 · KR-5 · Demos are `/review/mocks/<mock>/<kit>` (nine combinations); the bare `/review/mocks/<mock>` redirects to its paired kit · Taylor asked to try each layout in every kit; the kit is in the path, not a query, because comments are keyed by `usePathname()`. Supersedes KR-1..3's one-kit-per-mock pairing (the pairing survives as the default and the "designed for" mark).
2026-09-22 · KR-5 · Every review kit defines the same `MockVars` (`--link`, `--surface-dark`, `--surface-dark-foreground`, `--font-quote`, `--tag-*`), enforced by the `ReviewKit` type; mocks use `text-(--link)` for small accent text instead of `accent`/`primary` · kit C's `accent` is amber and its `primary` fails AA small on cream, so the mocks could not be moved between kits without a shared small-accent token. Kits A and B set the tag colours to their secondary grey (no coloured tags in cards A/B).
2026-09-22 · KR-5 · Layout pages no longer lead with the full markdown; a brief (`review/layouts/index.ts` `brief`) comes first and the deliverable is folded under a `<details>` · Taylor: too much text for a non-technical client. The deliverable is still rendered verbatim on the same page, which is what CB-1's M-CB-7 required.
2026-09-22 · KR-5 · `/review/brand` added (not in CB-1's route list) · the pillars (04) were the reasoning behind all three kits and appeared nowhere in the round.
2026-09-22 · KR-5 · `FrameRibbon` text is amber 800 on light kits, amber 300 on dark (`dark:`) · amber 300 on kit B's cream was unreadable; found when B's frames were shown in a light kit.
2026-09-22 · KR-6 · Answers travel as `answers: { schema, items[] }`, an ordered list of self-describing items, not the ticket's example of records keyed by id plus a label map · jsonb sorts object keys, so records lose question order in the stored row; see M-KR-4.
2026-09-22 · KR-6 · Cut from the kickoff's starting points: overall kit and layout ranks, a "sounds like me" slider per pillar (one slider for the set instead), the essence line keep/change/drop, a separate hero-line pick, social links · none would change the build beyond what the favourite pick, the per-part ranks and the forks already decide; reasons per item in the ticket's "Question set".
2026-09-22 · KR-6 · The re-asked sliders use the intake's own labels and ends from taylor-aucoin `lib/intake/showcase-copy.ts` (`tasteSpectrums`), not wording reconstructed from 02 · 02 records the values but only some of the end labels; the intake file is what he actually saw. Values from 02; "6/7" carried as 6.0.
2026-09-22 · KR-6 · The draft stays in the browser after sending (marked sent, with "Change an answer and send again") instead of being cleared · lets him revise; a re-send is a new submission id and a second email, which Taylor wants if answers changed.
2026-09-22 · KR-6 · The submission id is now minted in the browser and kept in the draft until a send is confirmed; the action reuses it · CB-1 minted a new id on every attempt, so a retry after a lost response would have been a second submission and a second email, against contract §1.
2026-09-22 · KR-6 · End-to-end send verified against a local capture server standing in for taylor-aucoin, not taylor-aucoin on :4300 · Taylor runs taylor-aucoin against staging Supabase only (no local database), and a staging send needs migration 0017 applied, a minted round and would send a real email; the captured payload was then run through taylor-aucoin's own validator and email formatter.
2026-09-22 · KR-6 · `docs/REVIEW-LAYER.md`'s route table re-padded by prettier when the feedback row changed · formatting only.
2026-09-22 · KR-6 · Per-question notes travel as separate `text` items with id `<question id>.note` rather than a `note` field on each answer · a note on an unanswered question has no answer to hang on; the separate item needs no new wire shape, keeps form order, and taylor-aucoin folds it back (`lib/review/answers.ts`).
2026-09-22 · KR-6 · Notes sit behind "Add a thought" except on the brand section · Taylor asked for a box on every question; twenty open boxes on a phone would break the ten-minute target and read as a survey.
2026-09-22 · KR-6 · The re-asked intake sliders (acceptance criterion 4, section 5 of the question set) are removed · Taylor: "we already have that data". The `baseline` wire field stays, sent as null.
2026-09-22 · KR-6 · Lane-name option reads "because I wanted to", not 03 §8's "because I had to" · Taylor: "had to" sounds wrong. The copy doc's fork is unchanged; this is the wording put to him.
2026-09-22 · KR-6 · `layout.nav` added (which menu), beyond the overview's four forks · Taylor asked to capture his thoughts on the three layouts' navigation; the note box is open for it.
2026-09-22 · KR-6 · `lib/review/client.ts` added and `REVIEW_INGEST_KEY` redefined as the shared sister-repo key (M-KR-5); REVIEW-LAYER §4's round setup, `.env.example`, the contract §1/§2/§5/§6 and KR-4's line in `00-build-order.md` updated · Taylor asked for a static client value and one key; taylor-aucoin's REV-4.
2026-09-24 · post-round (04 v2) · `review/brand.ts` and `/review/brand` amended to Card A v2 (pillars in his order: Wicked · Resourceful · Galvanizing; Generous retired; Resourceful guarded "never cheap"; overlap "slick on a shoestring" → "slick under pressure"). The brand page now opens with "Changed after your review" and shows his chosen kit instead of three leads; `KitLead.leads` became a display string (`lead`) plus `chosen`, so kits B and C keep what the round showed and their kit pages say "Shown in the round; not chosen." · Kryshan's review rejected Generous as "soft, people pleasing, under charging and not as assertive" (docs/client/kryshan-08-review-synthesis.md; 04 Step 8). Supersedes KR-5's three-leads section.
2026-09-24 · post-round (04 v2) · `review/feedback.ts` freezes the round's pillar options in a local `ROUND_1_PILLARS` instead of reading `PILLARS` · the brand was amended after he answered; a submitted form's options must not change under schema `kryshan-2026-09` (M-KR-4). The rendered options are identical to what he answered.
2026-09-24 · specs · `docs/specs/` split into numbered track folders: `01-review-round/` (KR-1..3, KR-5, KR-6 and their `00-build-order.md`) and `02-review-demo-d/` (Demo D's UX handoff). README, PROGRESS, DEVIATIONS, TECHNICAL-DECISIONS, `_templates/` and `spec-system-guide.md` stay at the root and serve every track · Taylor's instruction. The KR tickets and `docs/client/prompts/` cite `docs/specs/KR-*.md` at the old paths; they are frozen records and are not rewritten. `review/feedback.ts`'s pointer to KR-6 was updated.
2026-09-24 · KR-7 · `round` is optional on `ReviewKit`, `ReviewLayout` and `ReviewMock` (absent = 1, read through `roundOf`), not a field every entry sets · so kits A–C, layouts A–C and mocks A–C need no edit, and A's kit file stays untouched (D-KRD-1).
2026-09-24 · KR-7 · Layout A's brief moved into a `LAYOUT_A_BRIEF` constant in `review/layouts/index.ts`, shared by layouts A and D · D starts as a faithful copy of A (the ticket); the rendered brief for A is unchanged.
2026-09-24 · KR-7 · `CONTACT_EMAIL` was not added to `.env.local` (agents may not edit it); verification ran with the variable set inline · Taylor adds `CONTACT_EMAIL=kryshanrandel@gmail.com` to `.env.local` and to Vercel before the next local build or deploy.
2026-09-24 · KR-7 · Every consumer of `SITE.email` (Demo A included) now shows `CONTACT_EMAIL` · D-KRD-16 accepts this; the address is not what the before/after compares.
2026-09-24 · KR-8 · "For hire" still appears once in Demo D's DOM, inside his Directing strand copy ("sizzle reels for hire", 03 §8, from his old site) · D-KRD-10 bans "For hire" as a label or lane name; this is his own prose describing the work, not a label. Acceptance #4 is read as applying to labels, names and filters.
2026-09-24 · KR-8 · The panel's "player mounting" spinner and "embed failed" states (handoff §8) are not added; the shared `ProjectPlayer`/`VideoEmbed` behave as in Demo A · adding them means editing components A shares (D-KRD-1), and a cross-origin iframe's load failure can't be detected reliably. Listed as out of scope in the ticket.
2026-09-24 · KR-8 · Tile and panel transitions abort in a hidden tab ("invalid state") and apply instantly; `transition.ready` is caught so nothing is left unhandled · the browser skips view transitions while `visibilityState` is hidden; the state change still applies.
2026-09-24 · KR-8 · The layout page's comparison table and the kit page's links are scoped by round (layout D: "Before and after" with A; kit D links to A; A also links to D) · without it, adding D changed round-1 pages ("The three side by side" showed four). Not in the ticket's placement list.
2026-09-24 · KR-8 · The transition timing CSS lives in `home-d.tsx`, not in `transition.ts` · a server component may not import a module that imports `flushSync`.
2026-09-24 · KR-9 · Rows snap with `snap-proximity`, not `snap-mandatory`, and a focused tile is scrolled to the snap position (a tile's start) that shows it whole · with mandatory snapping, and with a plain scroll-into-view, the browser pulled a focused tile back to an earlier snap point and left it clipped (found in the keyboard walk). Handoff §6.6 said "mandatory".
2026-09-24 · KR-9 · At the row's ends the arrows are `invisible` (they keep their space) rather than removed · the other arrow doesn't shift under the pointer mid-scroll.
2026-09-24 · KR-9 · Keyboard focus walks were verified by dispatching `focusin` to each tile, because the agent's browser pane is a hidden, unfocused document (no native focus events or focus scrolling there) · real Tab presses in a focused window should be checked once by hand before D is sent.
2026-09-24 · KR-7 (correction) · `CONTACT_EMAIL` is `hello@kryshanrandel.com` (`.env.example`, handoff §3 and O-4), not his Gmail "until verified" · Taylor: a personal inbox on the site restricts his ability to change where mail goes; the domain alias can be re-pointed at the mail provider without touching the site. The alias must exist before D is sent (O-4). Supersedes the KR-7 ruling and the KR-7 `.env.local` line above.
2026-09-24 · KR-7 (correction) · `CONTACT_EMAIL` is optional with a fallback of `hello@kryshanrandel.com` (lib/env.ts), no longer required · Taylor: a missing variable must not break a Vercel build (a Preview deploy failed with it undefined). Supersedes the KR-7 ruling that it fail the build.
2026-09-24 · SITE · SITE tickets built in one Claude Code thread · Taylor's instruction; a hard checkpoint and context reset between tickets preserve ground truth.
2026-09-24 · SITE-1 · `review/mocks/home-a.tsx`, `home-b.tsx`, `home-c.tsx` and `home-d.tsx` edited: `SITE.social` → `SOCIALS` (from `content/site.ts`) and the import line, nothing else · D-SITE-28 moves socials out of `lib/config.ts`; the smallest change that keeps Demos A–D compiling. Their footers list the same links in the same order, and A and D render identically at 1440 and 390.
2026-09-24 · SITE-1 · The production kit drops Kit A/D's `tagline`, `letter`, `thesis`, `voice`, `never`, `round` and `placeholder`, and the `extraVars` `--surface-dark`, `--surface-dark-foreground` and `--tag-*` · `tagline` isn't a `BrandKit` field (the scope sheet's drop list omits it but keeping it fails type-checking); the three `--tag-*` and the dark surface are the review mocks' contract (M-KR-3) and D-KRD-19 forbids role colours.
2026-09-24 · SITE-1 · The production kit's `roles` copy Kit A's text verbatim, including "Accent: Wordmark, hover, active role tag…" and "Surface: Overlay backdrop, the contact card, the credits list column" · acceptance #1 requires the roles to match Kit D's rendered page. Both lines contradict the live rules (D-KRD-19: no role tags; spec §4.1: hovers are `--link`, `card` is the arrow and end-tile plates, a failed tile). Routed to SITE-9, which writes the style guide from these roles.
2026-09-24 · SITE-1 · "More pages" (the phone nav row's landmark name) added to `content/site.ts` `CHROME`; it is not in spec §7.6's microcopy table · Demo D's built label (KR-8); an addition to §7.6.
2026-09-24 · SITE-1 · `docs/specs/README.md`'s non-negotiable "the first poster `priority`" corrected to `preload`, with PERFORMANCE §4 · the same stale fact in a second place (spec §10's house doc fix; Next 16 deprecates `priority`).
2026-09-24 · SITE-1 · The D-SITE-19 wall also blocks the relative form (`**/review/*`), not only `@/review/*`, `@/lib/review/*` and `@/app/review/*` · a relative import is exactly how a review file slips into public code unnoticed (the ticket's ruling 11).
2026-09-24 · SITE-1 · Old-URL mapping: WordPress category pagination (`/project_category/<x>/page/<n>/`, 15 URLs found by the link harvest) is mapped as its category (`kind: "category"`, role kept for directing, camera and editing); the ticket's table names only `/project_category/<x>/` · a paged archive of a category is that category; mapping them to plain `/work` would drop the role the visitor was browsing. `[PROVISIONAL — Taylor]`, one data row each.
2026-09-24 · SITE-1 · `.env.example`'s `NEXT_PUBLIC_SITE_URL` is now empty (optional, fallback named in the comment), not `http://localhost:3000` · the ticket asked only for the comment, but a copied example would have kept pointing canonicals at localhost, which is what D-SITE-24 exists to prevent.
2026-09-24 · SITE-1 · Acceptance #18 expects kit C's "Frame to be replaced" ribbon in amber 800 as a light kit; kit C's ground is dark (carbon `#0C0E0D`), so its ribbon stays amber 300, as it was before this ticket · the criterion's premise is wrong about C; B is the only light kit, and its ribbon is amber 800 under the dark root (M-SITE-2).
2026-09-24 · SITE-1 · The bar's nav items are the Label step at 11 px (spec §4.1, the ticket) where Demo D's bar used `text-xs` (12 px) · the spec's type step governs the live site; Demo D is unchanged.
2026-09-24 · SITE-1 · The footer's Social `nav` holds both lines in one column (right-aligned at ≥768), where Demo D's footer put the Instagram · Facebook line in its own row under everything · the ticket makes both lines one `<nav aria-label="Social">` landmark, which can't span two flex rows; the visual is the same order and alignment.
2026-09-24 · SITE-1 · The current nav item's underline classes are joined to the item's classes without `cn()` · the `cn` package (shadcn's clsx + tailwind-merge) reads `decoration-(color:--link)` and `decoration-2` as one group and silently drops the colour (found in the browser walk). Any later ticket that sets a decoration colour and thickness together (SITE-4's filter links) hits the same trap.
2026-09-24 · SITE-2 · Ruling 2 took the fallback branch: no approved Directors Reel frame exists (no DEVIATIONS line, MANIFEST row or Taylor note names one; `[NEEDS VALUE AT BUILD]` read 2026-09-24), so `directors-reel` is held, with 22 public and 5 held · spec §6.1's fallback, applied now because `posterStatus` is gone and there is no honest "public, frame unapproved" state.
2026-09-24 · SITE-2 · In the fallback, Contact Club takes the Directors Reel's slot (position 2) in `FEATURED`, and Born To Be leads `DIRECTING_ROW` · spec §6.1 says "Contact Club moves up" without a position; the vacated slot keeps every other film where he approved it. Reversible in two lines (the comment in `content/home.ts` says how).
2026-09-24 · SITE-2 · Loglines are his intake words cut by the ticket's rule; where the Watch note names only the kind, the Story note's first sentence is used · the scope sheet names only Watch notes, but a kind-only line repeats the genre line (§6.3). All 27 are flagged for SITE-C in the file header; grammar and tense are left as he wrote them.
2026-09-24 · SITE-2 · The Wolf of West Georgia Street's logline drops the private person's name and inserts "a" ("…for a fortieth birthday party."), the one non-deletion · the intake spells the name two ways (§7.4: when sources disagree, say less), and he is a private person (§6.3).
2026-09-24 · SITE-2 · `content/validate.ts` enforces checks beyond spec §5's list: every public film has a video; link-outs are https and never on kryshanrandel.com; no slug twice across the Home lists; the red phrase appears once in the headline; testimonial counts per page; `LEGACY_PATHS` targets and uniqueness; "at-risk" in no testimonial role or photo words · each protects a binding rule (D-SITE-8, D-SITE-9 and O-6, §4.1, D-SITE-13, D-SITE-25, §11) that nothing else enforces. The "at-risk" match also catches "at risk" and "atrisk".
2026-09-24 · SITE-2 · The Bully Solution and Shotlister lost their old-site link-outs and carry no `embed` (both held); VANDU's `{ provider: "none" }` is gone the same way · D-SITE-9 and handoff O-6: after cutover an old-site link-out redirects to itself. The demos show none of the three, so no demo changes.
2026-09-24 · SITE-2 · `findProject` kept, for the review layer only (its doc comment says so); public code uses `findShowableProject` · the demos still show held films as he reviewed them. SITE-9 deletes it if nothing else imports it.
2026-09-24 · SITE-2 · New review file `review/mocks/_components/review-posters.ts` (`reviewPoster(slug)`: `POSTERS`, else a review-only static import of the five held films' old frames; `FRAME_TO_REPLACE`: Directors Reel, Glimpse, The Bully Solution) · replaces `Project.poster` and `posterStatus` for the demos without adding either back to the model.
2026-09-24 · SITE-2 · `review/mocks/_components/project-player.tsx` edited: poster from `reviewPoster`, the ribbon from `FRAME_TO_REPLACE`, `embed.provider === "none"` → `!embed`, doc comment updated · the type change makes the edit unavoidable. It is a Demo A component, so this departs from D-KRD-1 ("A's files aren't edited"); A renders identically (per-element fingerprint, 1440 and 390).
2026-09-24 · SITE-2 · `review/mocks/_components/expanding-grid.tsx` edited (Demo A's grid): `project.poster.src` → `reviewPoster(slug).src`, `posterStatus` → `FRAME_TO_REPLACE`, `embed.provider` → `embed?.provider` · as above; D-KRD-1 departed from for the same reason, with A unchanged on screen.
2026-09-24 · SITE-2 · `review/mocks/_components/expanding-index.tsx` edited (Demo C): the same poster and ribbon substitutions · the type change. C unchanged on screen.
2026-09-24 · SITE-2 · `review/mocks/_components/featured-lightbox.tsx` edited (Demo B): the same poster and ribbon substitutions · the type change. B unchanged on screen.
2026-09-24 · SITE-2 · `review/mocks/_components/reel-strip.tsx` edited (Demo B): `poster={project.poster}` → `reviewPoster(slug)`, ribbon from `FRAME_TO_REPLACE`, `embed?.provider` · the type change. B unchanged on screen.
2026-09-24 · SITE-2 · `review/mocks/_components/home-d/film-tile.tsx` edited (Demo D): fallback poster `reviewPoster(slug).src`, ribbon from `FRAME_TO_REPLACE`, `embed?.provider` · the type change. D unchanged on screen; `DEMO_D_POSTERS` untouched.
2026-09-24 · SITE-2 · The content-model ADR is M-SITE-3, not the M-SITE-2 the ticket named · SITE-1 had already logged M-SITE-2 (nested kit grounds). Later tickets citing "M-SITE-2" for the data shape mean M-SITE-3.
2026-09-24 · SITE-2 · Acceptance #1's grep also matches `lead.lead` in `app/review/(gated)/brand/page.tsx` and `kits/[kit]/page.tsx` · those are the review brand page's pillar-lead field (post-round 04 v2), not `Project.lead`, and are outside this ticket's adaptation list; the grep is clean outside `app/review/`.
2026-09-24 · SITE-3 · Demo D's leaves are copied into `components/composed/work/` (tile, panel, grid, row, player, open-film store, hook, transition) and rewritten there, so for now two copies of the film grammar exist, the other in `review/mocks/_components/home-d/` · D-SITE-19 forbids importing it and D-KRD-1 keeps the demo untouched; the review copy is deleted in SITE-9.
2026-09-24 · SITE-3 · Ruling 2: the open-film store resets `openSlug` and its session when its last subscriber unsubscribes · module state survives a client navigation, so a film left open would reopen and autoplay on Back. Verified with a same-document round trip (`/` → `/review` → Back): no panel, no iframe.
2026-09-24 · SITE-3 · Ruling 3: the transition timing (`::view-transition-group(*)` 220 ms, 150 ms under `html[data-film-vt=swap]`) and `--player-cap` (now `--bar-h` only) moved into an unlayered "Film components" block in `app/globals.css` · KR-8 kept them in the page because a server component can't import `flushSync`; Home and Work both need them. The rule is identical to the demos' own, so they render the same.
2026-09-24 · SITE-3 · Ruling 4 needed no change: SITE-1 had already put `data-site-bar` and `view-transition-name: site-bar` on the bar · the store measures the bar from `[data-site-bar]`'s `offsetHeight`.
2026-09-24 · SITE-3 · Ruling 5: `lib/pre-paint-script.ts` marks `<html data-js>` from an inline script first in `app/(site)/layout.tsx`; `html:not([data-js]) [data-needs-js] { display: none }` hides JavaScript-only controls (the copy button) without JavaScript, and the copy button also marks it on mount · the one path where the inline script doesn't run is a client navigation into `(site)` from outside it; React may log its "script tag" warning once there in development only.
2026-09-24 · SITE-3 · Ruling 6: the public film components carry no `data-review-id` and no `reviewPrefix` · nothing reviews the public pages through the comment layer, and SITE-9 deletes every `data-review-id` in public code (spec §2). Departs from CONVENTIONS §5.
2026-09-24 · SITE-3 · Ruling 7: `Person.jobTitle` is the roles line without its final full stop ("Director, camera operator, editor, and film instructor"); `addressLocality` is `SITE.place` up to its comma ("Vancouver") · spec §6.1 names the fields but not their values; both are his own styling, not new copy.
2026-09-24 · SITE-3 · Ruling 8: the arrows in "Watch ↓", "All {n} pieces →", "Teaching →", the end tiles and "Watch on {host} ↗" are `aria-hidden` spans; the visible text is unchanged · spec §4.3 states it for ↗ only; the others would be read as "right arrow".
2026-09-24 · SITE-3 · `CHROME.wordmarkName` in `content/site.ts` became a function of the name (`wordmarkName(SITE.name)`, passed by the bar), and the file's `@/lib/config` import is gone · SITE-3 requires `content/site.ts` to stay env-free because client leaves import it; SITE-1 had imported `SITE` there, which reads `CONTACT_EMAIL` and would throw in a client bundle.
2026-09-24 · SITE-3 · The `STRANDS` captions are the D-SITE-27 defaults and the O-SITE-5 Teaching fallback, so Demos A, C and D (which read `STRANDS`) now show the amended Directing, Camera and editing and Teaching lines; on D the longer Camera line wraps one more line and moves what's below it · the ticket expects this (acceptance 21). "For hire" no longer appears in Demo D's DOM, which retires KR-8's "for hire" line.
2026-09-24 · SITE-3 · Home renders the SITE-2 fallback lists: Contact Club is featured #2 and Born To Be leads the Directing row (7 films, not 8) · the Directors Reel is held (SITE-2 ruling 2); the ticket's defaults assume it is not.
2026-09-24 · SITE-3 · "Teaching →" and "All {n} pieces →" use `next/link`; the tiles and the end tiles are plain anchors · the ticket fixes plain anchors for tiles (no prefetch) and end tiles (Work's before-paint filter must run on arrival); the other two go to unfiltered pages, like the bar's links.
2026-09-24 · SITE-3 · `FilmPlayer` takes `preload` (for SITE-5's poster-first player) and passes it to `VideoEmbed`'s existing `priority` prop; `components/composed/media/` is unchanged · the build prints no deprecation warning, so the ticket's note leaves `VideoEmbed` alone.
2026-09-24 · SITE-3 · Home's canonical renders as the bare origin (`<link rel="canonical" href="https://kryshanrandel.com">`), not `…/` as acceptance 12 writes it · Next normalises the root path; the two are the same URL. The JSON-LD `url` keeps the trailing slash.
2026-09-24 · SITE-3 · The copy button re-announces its status with a 50 ms timeout, not `requestAnimationFrame` · a backgrounded or hidden document doesn't run animation frames, and the status must still update. A real clipboard write couldn't be exercised in the agent's unfocused pane (the browser rejects it there); the success path was verified with a stubbed `writeText`, the failure and share paths with real and stubbed rejections. Check once by hand in a focused window.
